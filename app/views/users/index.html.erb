<% if flash[:notice] %>
    <%= flash[:notice] %>
<% end %>


<% @user.each do |e|%>
    <p><%= e.uid %>
       <%= link_to '削除', "/users/#{e.id}" , method: :delete %>
    </p>
<% end %>

<p><%= link_to 'ユーザ登録', '/users/new' %></p>